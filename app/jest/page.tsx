import React from 'react';

function page() {
	return (
		<div>
			<h1>JEST TUTORIAL</h1>
		</div>
	)
};

export default page;

// This is a simple React functional component that renders a heading "JEST TUTORIAL" inside a div.
// It uses TypeScript for type safety by defining the component as a React.FC (Functional Component).
// The component is exported as the default export of the module, allowing it to be imported and used in other parts of the application.ّ